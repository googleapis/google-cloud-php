<?php
/*
 * Copyright 2026 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     https://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

/*
 * GENERATED CODE WARNING
 * Generated by gapic-generator-php from the file
 * https://github.com/googleapis/googleapis/blob/master/google/showcase/v1beta1/echo.proto
 * Updates to the above are reflected here through a refresh process.
 *
 * @experimental
 */

namespace Google\Showcase\V1beta1\Gapic;

use Google\ApiCore\ApiException;
use Google\ApiCore\Call;
use Google\ApiCore\CredentialsWrapper;
use Google\ApiCore\GapicClientTrait;
use Google\ApiCore\LongRunning\OperationsClient;
use Google\ApiCore\OperationResponse;
use Google\ApiCore\RequestParamsHeaderDescriptor;
use Google\ApiCore\RetrySettings;
use Google\ApiCore\Transport\TransportInterface;
use Google\ApiCore\ValidationException;
use Google\Auth\FetchAuthTokenInterface;
use Google\LongRunning\Operation;
use Google\Protobuf\Duration;
use Google\Protobuf\Timestamp;
use Google\Rpc\Status;
use Google\Showcase\V1beta1\BlockRequest;
use Google\Showcase\V1beta1\BlockResponse;
use Google\Showcase\V1beta1\EchoErrorDetailsRequest;
use Google\Showcase\V1beta1\EchoErrorDetailsResponse;
use Google\Showcase\V1beta1\EchoRequest;
use Google\Showcase\V1beta1\EchoResponse;
use Google\Showcase\V1beta1\ExpandRequest;
use Google\Showcase\V1beta1\FailEchoWithDetailsRequest;
use Google\Showcase\V1beta1\FailEchoWithDetailsResponse;
use Google\Showcase\V1beta1\PagedExpandLegacyMappedResponse;
use Google\Showcase\V1beta1\PagedExpandLegacyRequest;
use Google\Showcase\V1beta1\PagedExpandRequest;
use Google\Showcase\V1beta1\PagedExpandResponse;
use Google\Showcase\V1beta1\Severity;
use Google\Showcase\V1beta1\WaitRequest;
use Google\Showcase\V1beta1\WaitResponse;

/**
 * Service Description: This service is used showcase the four main types of rpcs - unary, server
 * side streaming, client side streaming, and bidirectional streaming. This
 * service also exposes methods that explicitly implement server delay, and
 * paginated calls. Set the 'showcase-trailer' metadata key on any method
 * to have the values echoed in the response trailers. Set the
 * 'x-goog-request-params' metadata key on any method to have the values
 * echoed in the response headers.
 *
 * This class provides the ability to make remote calls to the backing service through method
 * calls that map to API methods. Sample code to get started:
 *
 * ```
 * $echoClient = new EchoClient();
 * try {
 *     $response = $echoClient->block();
 * } finally {
 *     $echoClient->close();
 * }
 * ```
 *
 * @experimental
 *
 * @deprecated Please use the new service client {@see \Google\Showcase\V1beta1\Client\EchoClient}.
 */
class EchoGapicClient
{
    use GapicClientTrait;

    /** The name of the service. */
    const SERVICE_NAME = 'google.showcase.v1beta1.Echo';

    /** The default address of the service. */
    const SERVICE_ADDRESS = 'localhost:7469';

    /** The default port of the service. */
    const DEFAULT_SERVICE_PORT = 443;

    /** The name of the code generator, to be included in the agent header. */
    const CODEGEN_NAME = 'gapic';

    /** The default scopes required by the service. */
    public static $serviceScopes = [];

    private $operationsClient;

    private static function getClientDefaults()
    {
        return [
            'serviceName' => self::SERVICE_NAME,
            'apiEndpoint' => self::SERVICE_ADDRESS . ':' . self::DEFAULT_SERVICE_PORT,
            'clientConfig' => __DIR__ . '/../resources/echo_client_config.json',
            'descriptorsConfigPath' => __DIR__ . '/../resources/echo_descriptor_config.php',
            'gcpApiConfigPath' => __DIR__ . '/../resources/echo_grpc_config.json',
            'credentialsConfig' => [
                'defaultScopes' => self::$serviceScopes,
            ],
            'transportConfig' => [
                'rest' => [
                    'restClientConfigPath' => __DIR__ . '/../resources/echo_rest_client_config.php',
                ],
            ],
        ];
    }

    /**
     * Return an OperationsClient object with the same endpoint as $this.
     *
     * @return OperationsClient
     *
     * @experimental
     */
    public function getOperationsClient()
    {
        return $this->operationsClient;
    }

    /**
     * Resume an existing long running operation that was previously started by a long
     * running API method. If $methodName is not provided, or does not match a long
     * running API method, then the operation can still be resumed, but the
     * OperationResponse object will not deserialize the final response.
     *
     * @param string $operationName The name of the long running operation
     * @param string $methodName    The name of the method used to start the operation
     *
     * @return OperationResponse
     *
     * @experimental
     */
    public function resumeOperation($operationName, $methodName = null)
    {
        $options = isset($this->descriptors[$methodName]['longRunning']) ? $this->descriptors[$methodName]['longRunning'] : [];
        $operation = new OperationResponse($operationName, $this->getOperationsClient(), $options);
        $operation->reload();
        return $operation;
    }

    /**
     * Constructor.
     *
     * @param array $options {
     *     Optional. Options for configuring the service API wrapper.
     *
     *     @type string $apiEndpoint
     *           The address of the API remote host. May optionally include the port, formatted
     *           as "<uri>:<port>". Default 'localhost:7469:443'.
     *     @type string|array|FetchAuthTokenInterface|CredentialsWrapper $credentials
     *           The credentials to be used by the client to authorize API calls. This option
     *           accepts either a path to a credentials file, or a decoded credentials file as a
     *           PHP array.
     *           *Advanced usage*: In addition, this option can also accept a pre-constructed
     *           {@see \Google\Auth\FetchAuthTokenInterface} object or
     *           {@see \Google\ApiCore\CredentialsWrapper} object. Note that when one of these
     *           objects are provided, any settings in $credentialsConfig will be ignored.
     *     @type array $credentialsConfig
     *           Options used to configure credentials, including auth token caching, for the
     *           client. For a full list of supporting configuration options, see
     *           {@see \Google\ApiCore\CredentialsWrapper::build()} .
     *     @type bool $disableRetries
     *           Determines whether or not retries defined by the client configuration should be
     *           disabled. Defaults to `false`.
     *     @type string|array $clientConfig
     *           Client method configuration, including retry settings. This option can be either
     *           a path to a JSON file, or a PHP array containing the decoded JSON data. By
     *           default this settings points to the default client config file, which is
     *           provided in the resources folder.
     *     @type string|TransportInterface $transport
     *           The transport used for executing network requests. May be either the string
     *           `rest` or `grpc`. Defaults to `grpc` if gRPC support is detected on the system.
     *           *Advanced usage*: Additionally, it is possible to pass in an already
     *           instantiated {@see \Google\ApiCore\Transport\TransportInterface} object. Note
     *           that when this object is provided, any settings in $transportConfig, and any
     *           $apiEndpoint setting, will be ignored.
     *     @type array $transportConfig
     *           Configuration options that will be used to construct the transport. Options for
     *           each supported transport type should be passed in a key for that transport. For
     *           example:
     *           $transportConfig = [
     *               'grpc' => [...],
     *               'rest' => [...],
     *           ];
     *           See the {@see \Google\ApiCore\Transport\GrpcTransport::build()} and
     *           {@see \Google\ApiCore\Transport\RestTransport::build()} methods for the
     *           supported options.
     *     @type callable $clientCertSource
     *           A callable which returns the client cert as a string. This can be used to
     *           provide a certificate and private key to the transport layer for mTLS.
     * }
     *
     * @throws ValidationException
     *
     * @experimental
     */
    public function __construct(array $options = [])
    {
        $clientOptions = $this->buildClientOptions($options);
        $this->setClientOptions($clientOptions);
        $this->operationsClient = $this->createOperationsClient($clientOptions);
    }

    /**
     * This method will block (wait) for the requested amount of time
     * and then return the response or error.
     * This method showcases how a client handles delays or retries.
     *
     * Sample code:
     * ```
     * $echoClient = new EchoClient();
     * try {
     *     $response = $echoClient->block();
     * } finally {
     *     $echoClient->close();
     * }
     * ```
     *
     * @param array $optionalArgs {
     *     Optional.
     *
     *     @type Duration $responseDelay
     *           The amount of time to block before returning a response.
     *     @type Status $error
     *           The error that will be returned by the server. If this code is specified
     *           to be the OK rpc code, an empty response will be returned.
     *     @type BlockResponse $success
     *           The response to be returned that will signify successful method call.
     *     @type RetrySettings|array $retrySettings
     *           Retry settings to use for this call. Can be a {@see RetrySettings} object, or an
     *           associative array of retry settings parameters. See the documentation on
     *           {@see RetrySettings} for example usage.
     * }
     *
     * @return \Google\Showcase\V1beta1\BlockResponse
     *
     * @throws ApiException if the remote call fails
     *
     * @experimental
     */
    public function block(array $optionalArgs = [])
    {
        $request = new BlockRequest();
        if (isset($optionalArgs['responseDelay'])) {
            $request->setResponseDelay($optionalArgs['responseDelay']);
        }

        if (isset($optionalArgs['error'])) {
            $request->setError($optionalArgs['error']);
        }

        if (isset($optionalArgs['success'])) {
            $request->setSuccess($optionalArgs['success']);
        }

        return $this->startCall('Block', BlockResponse::class, $optionalArgs, $request)->wait();
    }

    /**
     * This method, upon receiving a request on the stream, will pass the same
     * content back on the stream. This method showcases bidirectional
     * streaming RPCs.
     *
     * Sample code:
     * ```
     * $echoClient = new EchoClient();
     * try {
     *     $request = new EchoRequest();
     *     // Write all requests to the server, then read all responses until the
     *     // stream is complete
     *     $requests = [
     *         $request,
     *     ];
     *     $stream = $echoClient->chat();
     *     $stream->writeAll($requests);
     *     foreach ($stream->closeWriteAndReadAll() as $element) {
     *         // doSomethingWith($element);
     *     }
     *     // Alternatively:
     *     // Write requests individually, making read() calls if
     *     // required. Call closeWrite() once writes are complete, and read the
     *     // remaining responses from the server.
     *     $requests = [
     *         $request,
     *     ];
     *     $stream = $echoClient->chat();
     *     foreach ($requests as $request) {
     *         $stream->write($request);
     *         // if required, read a single response from the stream
     *         $element = $stream->read();
     *         // doSomethingWith($element)
     *     }
     *     $stream->closeWrite();
     *     $element = $stream->read();
     *     while (!is_null($element)) {
     *         // doSomethingWith($element)
     *         $element = $stream->read();
     *     }
     * } finally {
     *     $echoClient->close();
     * }
     * ```
     *
     * @param array $optionalArgs {
     *     Optional.
     *
     *     @type int $timeoutMillis
     *           Timeout to use for this call.
     * }
     *
     * @return \Google\ApiCore\BidiStream
     *
     * @throws ApiException if the remote call fails
     *
     * @experimental
     */
    public function chat(array $optionalArgs = [])
    {
        return $this->startCall('Chat', EchoResponse::class, $optionalArgs, null, Call::BIDI_STREAMING_CALL);
    }

    /**
     * This method will collect the words given to it. When the stream is closed
     * by the client, this method will return the a concatenation of the strings
     * passed to it. This method showcases client-side streaming RPCs.
     *
     * Sample code:
     * ```
     * $echoClient = new EchoClient();
     * try {
     *     $request = new EchoRequest();
     *     // Write data to server and wait for a response
     *     $requests = [
     *         $request,
     *     ];
     *     $stream = $echoClient->collect();
     *     $result = $stream->writeAllAndReadResponse($requests);
     *     // doSomethingWith($result)
     *     // Alternatively:
     *     // Write data as it becomes available, then wait for a response
     *     $requests = [
     *         $request,
     *     ];
     *     $stream = $echoClient->collect();
     *     foreach ($requests as $request) {
     *         $stream->write($request);
     *     }
     *     $result = $stream->readResponse();
     *     // doSomethingWith($result)
     * } finally {
     *     $echoClient->close();
     * }
     * ```
     *
     * @param array $optionalArgs {
     *     Optional.
     *
     *     @type int $timeoutMillis
     *           Timeout to use for this call.
     * }
     *
     * @return \Google\ApiCore\ClientStream
     *
     * @throws ApiException if the remote call fails
     *
     * @experimental
     */
    public function collect(array $optionalArgs = [])
    {
        return $this->startCall('Collect', EchoResponse::class, $optionalArgs, null, Call::CLIENT_STREAMING_CALL);
    }

    /**
     * This method simply echoes the request. This method showcases unary RPCs.
     *
     * Sample code:
     * ```
     * $echoClient = new EchoClient();
     * try {
     *     $response = $echoClient->echo();
     * } finally {
     *     $echoClient->close();
     * }
     * ```
     *
     * @param array $optionalArgs {
     *     Optional.
     *
     *     @type string $content
     *           The content to be echoed by the server.
     *     @type Status $error
     *           The error to be thrown by the server.
     *     @type int $severity
     *           The severity to be echoed by the server.
     *           For allowed values, use constants defined on {@see \Google\Showcase\V1beta1\Severity}
     *     @type string $header
     *           Optional. This field can be set to test the routing annotation on the Echo method.
     *     @type string $otherHeader
     *           Optional. This field can be set to test the routing annotation on the Echo method.
     *     @type string $requestId
     *           To facilitate testing of https://google.aip.dev/client-libraries/4235
     *     @type string $otherRequestId
     *           To facilitate testing of https://google.aip.dev/client-libraries/4235
     *     @type RetrySettings|array $retrySettings
     *           Retry settings to use for this call. Can be a {@see RetrySettings} object, or an
     *           associative array of retry settings parameters. See the documentation on
     *           {@see RetrySettings} for example usage.
     * }
     *
     * @return \Google\Showcase\V1beta1\EchoResponse
     *
     * @throws ApiException if the remote call fails
     *
     * @experimental
     */
    public function echo(array $optionalArgs = [])
    {
        $request = new EchoRequest();
        $requestParamHeaders = [];
        if (isset($optionalArgs['content'])) {
            $request->setContent($optionalArgs['content']);
        }

        if (isset($optionalArgs['error'])) {
            $request->setError($optionalArgs['error']);
        }

        if (isset($optionalArgs['severity'])) {
            $request->setSeverity($optionalArgs['severity']);
        }

        if (isset($optionalArgs['header'])) {
            $request->setHeader($optionalArgs['header']);
            $requestParamHeaders['header'] = $optionalArgs['header'];
            $requestParamHeaders['routing_id'] = $optionalArgs['header'];
            $tableNameMatches = [];
            if (preg_match('/^(?<table_name>regions\/[^\/]+\/zones\/[^\/]+(?:\/.*)?)$/', $optionalArgs['header'], $tableNameMatches)) {
                $requestParamHeaders['table_name'] = $tableNameMatches['table_name'];
            } elseif (preg_match('/^(?<table_name>projects\/[^\/]+\/instances\/[^\/]+(?:\/.*)?)$/', $optionalArgs['header'], $tableNameMatches)) {
                $requestParamHeaders['table_name'] = $tableNameMatches['table_name'];
            }

            $superIdMatches = [];
            if (preg_match('/^(?<super_id>projects\/[^\/]+)(?:\/.*)?$/', $optionalArgs['header'], $superIdMatches)) {
                $requestParamHeaders['super_id'] = $superIdMatches['super_id'];
            }

            $instanceIdMatches = [];
            if (preg_match('/^projects\/[^\/]+\/(?<instance_id>instances\/[^\/]+)(?:\/.*)?$/', $optionalArgs['header'], $instanceIdMatches)) {
                $requestParamHeaders['instance_id'] = $instanceIdMatches['instance_id'];
            }
        }

        if (isset($optionalArgs['otherHeader'])) {
            $request->setOtherHeader($optionalArgs['otherHeader']);
            $requestParamHeaders['baz'] = $optionalArgs['otherHeader'];
            $quxMatches = [];
            if (preg_match('/^(?<qux>projects\/[^\/]+)(?:\/.*)?$/', $optionalArgs['otherHeader'], $quxMatches)) {
                $requestParamHeaders['qux'] = $quxMatches['qux'];
            }
        }

        if (isset($optionalArgs['requestId'])) {
            $request->setRequestId($optionalArgs['requestId']);
        }

        if (isset($optionalArgs['otherRequestId'])) {
            $request->setOtherRequestId($optionalArgs['otherRequestId']);
        }

        $requestParams = new RequestParamsHeaderDescriptor($requestParamHeaders);
        $optionalArgs['headers'] = isset($optionalArgs['headers']) ? array_merge($requestParams->getHeader(), $optionalArgs['headers']) : $requestParams->getHeader();
        return $this->startCall('Echo', EchoResponse::class, $optionalArgs, $request)->wait();
    }

    /**
     * This method returns error details in a repeated "google.protobuf.Any"
     * field. This method showcases handling errors thus encoded, particularly
     * over REST transport. Note that GAPICs only allow the type
     * "google.protobuf.Any" for field paths ending in "error.details", and, at
     * run-time, the actual types for these fields must be one of the types in
     * google/rpc/error_details.proto.
     *
     * Sample code:
     * ```
     * $echoClient = new EchoClient();
     * try {
     *     $response = $echoClient->echoErrorDetails();
     * } finally {
     *     $echoClient->close();
     * }
     * ```
     *
     * @param array $optionalArgs {
     *     Optional.
     *
     *     @type string $singleDetailText
     *           Content to return in a singular `*.error.details` field of type
     *           `google.protobuf.Any`
     *     @type string[] $multiDetailText
     *           Content to return in a repeated `*.error.details` field of type
     *           `google.protobuf.Any`
     *     @type RetrySettings|array $retrySettings
     *           Retry settings to use for this call. Can be a {@see RetrySettings} object, or an
     *           associative array of retry settings parameters. See the documentation on
     *           {@see RetrySettings} for example usage.
     * }
     *
     * @return \Google\Showcase\V1beta1\EchoErrorDetailsResponse
     *
     * @throws ApiException if the remote call fails
     *
     * @experimental
     */
    public function echoErrorDetails(array $optionalArgs = [])
    {
        $request = new EchoErrorDetailsRequest();
        if (isset($optionalArgs['singleDetailText'])) {
            $request->setSingleDetailText($optionalArgs['singleDetailText']);
        }

        if (isset($optionalArgs['multiDetailText'])) {
            $request->setMultiDetailText($optionalArgs['multiDetailText']);
        }

        return $this->startCall('EchoErrorDetails', EchoErrorDetailsResponse::class, $optionalArgs, $request)->wait();
    }

    /**
     * This method splits the given content into words and will pass each word back
     * through the stream. This method showcases server-side streaming RPCs.
     *
     * Sample code:
     * ```
     * $echoClient = new EchoClient();
     * try {
     *     // Read all responses until the stream is complete
     *     $stream = $echoClient->expand();
     *     foreach ($stream->readAll() as $element) {
     *         // doSomethingWith($element);
     *     }
     * } finally {
     *     $echoClient->close();
     * }
     * ```
     *
     * @param array $optionalArgs {
     *     Optional.
     *
     *     @type string $content
     *           The content that will be split into words and returned on the stream.
     *     @type Status $error
     *           The error that is thrown after all words are sent on the stream.
     *     @type Duration $streamWaitTime
     *           The wait time between each server streaming messages
     *     @type int $timeoutMillis
     *           Timeout to use for this call.
     * }
     *
     * @return \Google\ApiCore\ServerStream
     *
     * @throws ApiException if the remote call fails
     *
     * @experimental
     */
    public function expand(array $optionalArgs = [])
    {
        $request = new ExpandRequest();
        if (isset($optionalArgs['content'])) {
            $request->setContent($optionalArgs['content']);
        }

        if (isset($optionalArgs['error'])) {
            $request->setError($optionalArgs['error']);
        }

        if (isset($optionalArgs['streamWaitTime'])) {
            $request->setStreamWaitTime($optionalArgs['streamWaitTime']);
        }

        return $this->startCall('Expand', EchoResponse::class, $optionalArgs, $request, Call::SERVER_STREAMING_CALL);
    }

    /**
     * This method always fails with a gRPC "Aborted" error status that contains
     * multiple error details.  These include one instance of each of the standard
     * ones in error_details.proto
     * (https://github.com/googleapis/googleapis/blob/master/google/rpc/error_details.proto)
     * plus a custom, Showcase-defined PoetryError. The intent of this RPC is to
     * verify that GAPICs can process these various error details and surface them
     * to the user in an idiomatic form.
     *
     * Sample code:
     * ```
     * $echoClient = new EchoClient();
     * try {
     *     $response = $echoClient->failEchoWithDetails();
     * } finally {
     *     $echoClient->close();
     * }
     * ```
     *
     * @param array $optionalArgs {
     *     Optional.
     *
     *     @type string $message
     *           Optional message to echo back in the PoetryError. If empty, a value will be
     *           provided.
     *     @type RetrySettings|array $retrySettings
     *           Retry settings to use for this call. Can be a {@see RetrySettings} object, or an
     *           associative array of retry settings parameters. See the documentation on
     *           {@see RetrySettings} for example usage.
     * }
     *
     * @return \Google\Showcase\V1beta1\FailEchoWithDetailsResponse
     *
     * @throws ApiException if the remote call fails
     *
     * @experimental
     */
    public function failEchoWithDetails(array $optionalArgs = [])
    {
        $request = new FailEchoWithDetailsRequest();
        if (isset($optionalArgs['message'])) {
            $request->setMessage($optionalArgs['message']);
        }

        return $this->startCall('FailEchoWithDetails', FailEchoWithDetailsResponse::class, $optionalArgs, $request)->wait();
    }

    /**
     * This is similar to the Expand method but instead of returning a stream of
     * expanded words, this method returns a paged list of expanded words.
     *
     * Sample code:
     * ```
     * $echoClient = new EchoClient();
     * try {
     *     $content = 'content';
     *     // Iterate over pages of elements
     *     $pagedResponse = $echoClient->pagedExpand($content);
     *     foreach ($pagedResponse->iteratePages() as $page) {
     *         foreach ($page as $element) {
     *             // doSomethingWith($element);
     *         }
     *     }
     *     // Alternatively:
     *     // Iterate through all elements
     *     $pagedResponse = $echoClient->pagedExpand($content);
     *     foreach ($pagedResponse->iterateAllElements() as $element) {
     *         // doSomethingWith($element);
     *     }
     * } finally {
     *     $echoClient->close();
     * }
     * ```
     *
     * @param string $content      The string to expand.
     * @param array  $optionalArgs {
     *     Optional.
     *
     *     @type int $pageSize
     *           The maximum number of resources contained in the underlying API
     *           response. The API may return fewer values in a page, even if
     *           there are additional values to be retrieved.
     *     @type string $pageToken
     *           A page token is used to specify a page of values to be returned.
     *           If no page token is specified (the default), the first page
     *           of values will be returned. Any page token used here must have
     *           been generated by a previous call to the API.
     *     @type RetrySettings|array $retrySettings
     *           Retry settings to use for this call. Can be a {@see RetrySettings} object, or an
     *           associative array of retry settings parameters. See the documentation on
     *           {@see RetrySettings} for example usage.
     * }
     *
     * @return \Google\ApiCore\PagedListResponse
     *
     * @throws ApiException if the remote call fails
     *
     * @experimental
     */
    public function pagedExpand($content, array $optionalArgs = [])
    {
        $request = new PagedExpandRequest();
        $request->setContent($content);
        if (isset($optionalArgs['pageSize'])) {
            $request->setPageSize($optionalArgs['pageSize']);
        }

        if (isset($optionalArgs['pageToken'])) {
            $request->setPageToken($optionalArgs['pageToken']);
        }

        return $this->getPagedListResponse('PagedExpand', $optionalArgs, PagedExpandResponse::class, $request);
    }

    /**
     * This is similar to the PagedExpand except that it uses
     * max_results instead of page_size, as some legacy APIs still
     * do. New APIs should NOT use this pattern.
     *
     * Sample code:
     * ```
     * $echoClient = new EchoClient();
     * try {
     *     $content = 'content';
     *     $response = $echoClient->pagedExpandLegacy($content);
     * } finally {
     *     $echoClient->close();
     * }
     * ```
     *
     * @param string $content      The string to expand.
     * @param array  $optionalArgs {
     *     Optional.
     *
     *     @type int $maxResults
     *           The number of words to returned in each page.
     *           (-- aip.dev/not-precedent: This is a legacy, non-standard pattern that
     *           violates aip.dev/158. Ordinarily, this should be page_size. --)
     *     @type string $pageToken
     *           The position of the page to be returned.
     *     @type RetrySettings|array $retrySettings
     *           Retry settings to use for this call. Can be a {@see RetrySettings} object, or an
     *           associative array of retry settings parameters. See the documentation on
     *           {@see RetrySettings} for example usage.
     * }
     *
     * @return \Google\Showcase\V1beta1\PagedExpandResponse
     *
     * @throws ApiException if the remote call fails
     *
     * @experimental
     */
    public function pagedExpandLegacy($content, array $optionalArgs = [])
    {
        $request = new PagedExpandLegacyRequest();
        $request->setContent($content);
        if (isset($optionalArgs['maxResults'])) {
            $request->setMaxResults($optionalArgs['maxResults']);
        }

        if (isset($optionalArgs['pageToken'])) {
            $request->setPageToken($optionalArgs['pageToken']);
        }

        return $this->startCall('PagedExpandLegacy', PagedExpandResponse::class, $optionalArgs, $request)->wait();
    }

    /**
     * This method returns a map containing lists of words that appear in the input, keyed by their
     * initial character. The only words returned are the ones included in the current page,
     * as determined by page_token and page_size, which both refer to the word indices in the
     * input. This paging result consisting of a map of lists is a pattern used by some legacy
     * APIs. New APIs should NOT use this pattern.
     *
     * Sample code:
     * ```
     * $echoClient = new EchoClient();
     * try {
     *     $content = 'content';
     *     $response = $echoClient->pagedExpandLegacyMapped($content);
     * } finally {
     *     $echoClient->close();
     * }
     * ```
     *
     * @param string $content      The string to expand.
     * @param array  $optionalArgs {
     *     Optional.
     *
     *     @type int $pageSize
     *           The number of words to returned in each page.
     *     @type string $pageToken
     *           The position of the page to be returned.
     *     @type RetrySettings|array $retrySettings
     *           Retry settings to use for this call. Can be a {@see RetrySettings} object, or an
     *           associative array of retry settings parameters. See the documentation on
     *           {@see RetrySettings} for example usage.
     * }
     *
     * @return \Google\Showcase\V1beta1\PagedExpandLegacyMappedResponse
     *
     * @throws ApiException if the remote call fails
     *
     * @experimental
     */
    public function pagedExpandLegacyMapped($content, array $optionalArgs = [])
    {
        $request = new PagedExpandRequest();
        $request->setContent($content);
        if (isset($optionalArgs['pageSize'])) {
            $request->setPageSize($optionalArgs['pageSize']);
        }

        if (isset($optionalArgs['pageToken'])) {
            $request->setPageToken($optionalArgs['pageToken']);
        }

        return $this->startCall('PagedExpandLegacyMapped', PagedExpandLegacyMappedResponse::class, $optionalArgs, $request)->wait();
    }

    /**
     * This method will wait for the requested amount of time and then return.
     * This method showcases how a client handles a request timeout.
     *
     * Sample code:
     * ```
     * $echoClient = new EchoClient();
     * try {
     *     $operationResponse = $echoClient->wait();
     *     $operationResponse->pollUntilComplete();
     *     if ($operationResponse->operationSucceeded()) {
     *         $result = $operationResponse->getResult();
     *         // doSomethingWith($result)
     *     } else {
     *         $error = $operationResponse->getError();
     *         // handleError($error)
     *     }
     *     // Alternatively:
     *     // start the operation, keep the operation name, and resume later
     *     $operationResponse = $echoClient->wait();
     *     $operationName = $operationResponse->getName();
     *     // ... do other work
     *     $newOperationResponse = $echoClient->resumeOperation($operationName, 'wait');
     *     while (!$newOperationResponse->isDone()) {
     *         // ... do other work
     *         $newOperationResponse->reload();
     *     }
     *     if ($newOperationResponse->operationSucceeded()) {
     *         $result = $newOperationResponse->getResult();
     *         // doSomethingWith($result)
     *     } else {
     *         $error = $newOperationResponse->getError();
     *         // handleError($error)
     *     }
     * } finally {
     *     $echoClient->close();
     * }
     * ```
     *
     * @param array $optionalArgs {
     *     Optional.
     *
     *     @type Timestamp $endTime
     *           The time that this operation will complete.
     *     @type Duration $ttl
     *           The duration of this operation.
     *     @type Status $error
     *           The error that will be returned by the server. If this code is specified
     *           to be the OK rpc code, an empty response will be returned.
     *     @type WaitResponse $success
     *           The response to be returned on operation completion.
     *     @type RetrySettings|array $retrySettings
     *           Retry settings to use for this call. Can be a {@see RetrySettings} object, or an
     *           associative array of retry settings parameters. See the documentation on
     *           {@see RetrySettings} for example usage.
     * }
     *
     * @return \Google\ApiCore\OperationResponse
     *
     * @throws ApiException if the remote call fails
     *
     * @experimental
     */
    public function wait(array $optionalArgs = [])
    {
        $request = new WaitRequest();
        if (isset($optionalArgs['endTime'])) {
            $request->setEndTime($optionalArgs['endTime']);
        }

        if (isset($optionalArgs['ttl'])) {
            $request->setTtl($optionalArgs['ttl']);
        }

        if (isset($optionalArgs['error'])) {
            $request->setError($optionalArgs['error']);
        }

        if (isset($optionalArgs['success'])) {
            $request->setSuccess($optionalArgs['success']);
        }

        return $this->startOperationsCall('Wait', $optionalArgs, $request, $this->getOperationsClient())->wait();
    }
}
